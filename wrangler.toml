# The Trace Manifest — Pages config (Astro site only)
# Ingestion Worker is configured separately in wrangler.worker.toml

name = "the-trace-manifest"
compatibility_date = "2026-07-12"
compatibility_flags = ["nodejs_compat"]

# Astro Pages deployment
[env.production]
name = "the-trace-manifest"
